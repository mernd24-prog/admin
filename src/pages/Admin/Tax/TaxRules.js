import React from "react";
import ComingSoonPage from "../../../components/Shared/ComingSoonPage";

const TaxRules = () => (
  <ComingSoonPage
    title="Tax Rules"
    description="Configure tax rules by category, region, and structure. Backend API integration is pending."
  />
);

export default TaxRules;
